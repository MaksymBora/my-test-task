export const dropdownStyles = {
  control: (styles) => ({
    ...styles,
    cursor: 'pointer',
    fontSize: '15px',
    background: '#F7F7FB',
    border: 'none',
    borderRadius: '14px',
    padding: '6px 8px',
  }),
  option: (styles) => ({
    ...styles,
    cursor: 'pointer',
    fontSize: '14px',
  }),
};
